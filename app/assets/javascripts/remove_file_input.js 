
function removeFileInput() {
  $('.remove_file_field').click(function(e) {
    debugger;
    e.preventDefault();
    $(this).parent().remove();
  });
};

$(document).on('turbolinks:load', removeFileInput);
$(document).on('ajax:success', removeFileInput);
$(document).on('cocoon:after-insert', removeFileInput);
